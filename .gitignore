# Generado por XcodeGen — no se versiona
*.xcodeproj
*.xcworkspace

# Xcode
build/
# Salida del simulador y de las capturas: pesa cientos de megas y se regenera.
build-sim/
.spm/
DerivedData/
xcuserdata/
*.xcuserstate
.DS_Store

# Dependencias resueltas por SwiftPM
.spm/

# Credencial del demo: fuera del repo
Local.xcconfig

# El .ipa y el archive son artefactos de build
build/

# Basura de Python al usar scripts/asc.py
__pycache__/

# El último número de build que subí; ver subir-testflight.sh
.ultima-build
